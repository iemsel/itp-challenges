<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>HTTP Cat Gallery</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-[#7f927b] text-gray-800 min-h-screen flex flex-col">

  <%- include('./partials/nav.ejs') %>

  <header class="text-center mt-6 mb-4 border-b border-white pb-2 max-w-5xl mx-auto px-4">
    <h1 class="text-3xl font-semibold font-mono">
      Cat Codes (^-x-^)
    </h1>
  </header>

  <main class="flex-grow max-w-5xl mx-auto px-4 pb-10">
    <section aria-label="HTTP Cat Codes Gallery">
      <ul class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-6 list-none p-0 m-0">
        <% codes.forEach(code => { %>
          <li>
            <a href="/code/<%= code.code %>" class="block group rounded overflow-hidden shadow hover:shadow-lg transition">
              <figure>
                <img
                  src="<%= code.imageUrl %>"
                  alt="HTTP Cat image for status code <%= code.code %>"
                  class="w-full h-64 object-cover"
                />
                <figcaption class="bg-black text-white text-center py-2 text-sm font-mono group-hover:bg-gray-800 transition">
                  Code: <%= code.code %>
                </figcaption>
              </figure>
            </a>
          </li>
        <% }) %>
      </ul>
    </section>
  </main>

</body>
</html>
