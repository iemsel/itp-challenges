<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>HTTP Cat <%= code.code %></title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-[#7f927b] text-gray-800 min-h-screen flex flex-col">

  <%- include('partials/nav.ejs') %>

  <header class="text-center mt-6 mb-4 border-b border-white pb-2 max-w-5xl mx-auto px-4">
    <h1 class="text-3xl font-bold">HTTP Cat Code: <%= code.code %></h1>
  </header>

  <main class="flex-grow flex flex-col md:flex-row items-center justify-center gap-10 p-6 max-w-5xl mx-auto">

    <section class="max-w-md text-center md:text-left">
      <% if (code.imageUrl) { %>
        <img src="<%= code.imageUrl %>" alt="HTTP Cat <%= code.code %>" class="w-96 shadow" />
      <% } else { %>
        <figure class="w-96 h-60 flex items-center justify-center bg-gray-300 rounded-lg shadow">
          <figcaption class="text-lg font-semibold text-gray-600">Image not available</figcaption>
        </figure>
      <% } %>
    </section>

    <section class="max-w-md text-center md:text-left">
      <p class="text-lg"><strong>Description:</strong> <%= description %></p>
    </section>

  </main>

</body>
</html>
